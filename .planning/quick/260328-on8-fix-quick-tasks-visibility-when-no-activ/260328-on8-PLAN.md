---
phase: quick-260328-on8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/tree/view.go
  - internal/tui/tree/model_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "When all milestones are archived and quick tasks exist, the empty state shows the quick tasks tree (header + rows) below the archived message"
    - "When all milestones are archived and no quick tasks exist, the empty state shows the 'or run a quick task' hint"
    - "In both archived-milestone empty states the placeholder message is preceded by 2 blank lines of vertical spacing"
    - "The 'No GSD project found' branch is unchanged"
  artifacts:
    - path: "internal/tui/tree/view.go"
      provides: "Updated empty-state rendering in View()"
    - path: "internal/tui/tree/model_test.go"
      provides: "TestView_ArchivedOnlyWithQuickTasks covering new branch"
---

<objective>
Fix empty-state rendering when all milestones are archived:
1. Add 2 blank lines of vertical spacing before the archived message.
2. When quick tasks exist, replace the static "or run a quick task" hint with the actual rendered quick tasks tree.

Purpose: Quick tasks are invisible when no active milestone exists — the pane shows a static hint instead of the live task list.
Output: Updated View() empty-state block in internal/tui/tree/view.go, plus a new test covering the quick-tasks-present branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update empty-state rendering for archived-milestone case</name>
  <files>internal/tui/tree/view.go, internal/tui/tree/model_test.go</files>
  <action>
In View() at the `len(t.data.Phases) == 0` block (lines ~109-126), replace the archived-milestones branch with the following logic:

**Change 1 — vertical spacing:**
Prefix the archived message string with `"\n\n"` so the placeholder is pushed down from the top of the pane.

**Change 2 — conditional quick tasks rendering:**
After building the centered+padded placeholder lines, check `len(t.data.QuickTasks) > 0`:

If NO quick tasks: keep the existing static suffix `"or run a quick task:\n/gsd:quick"` in the msg string (but still add the `"\n\n"` prefix per Change 1). No structural change needed.

If quick tasks EXIST:
- Shorten msg to just `"\n\nAll milestones archived.\n\nStart a new milestone:\n/gsd:new-milestone"` (drop the "or run a quick task" suffix).
- Render and center/pad the placeholder text as before.
- Then build the quick tasks section inline (append to the same `padded` slice):
  - Header line: `lipgloss.NewStyle().Foreground(th.ExpandIndicatorFg).Render("▼ ") + "Quick tasks"`
  - For each `qt` in `t.data.QuickTasks`, index `qi`:
    - `isLast := qi == len(t.data.QuickTasks)-1`
    - `rawConnector` = `"    └── "` if isLast else `"    ├── "`
    - `connStyled := lipgloss.NewStyle().Foreground(th.ConnectorFg).Render(rawConnector)` (but override with `th.Pending.Render(rawConnector)` when `qt.Status == parser.StatusComplete`)
    - `icon := tui.StatusIcon(qt.Status, t.opts.NoEmoji, th)`
    - `prefixWidth := lipgloss.Width(rawConnector) + lipgloss.Width(icon) + 1`
    - `wrapWidth := width - 2 - prefixWidth` (min 1)
    - `nameParts := tui.WordWrap(qt.DisplayName, wrapWidth)`
    - For continuation lines (j > 0) use `strings.Repeat(" ", prefixWidth)` (no connector needed — this is empty state, not live tree).
    - Apply `th.Pending.Render(text)` when `qt.Status == parser.StatusComplete`, else plain `text`.
    - Prepend `" "` (the same 1-char left-padding as the padded slice) to each assembled line before appending.
- Append an empty string `""` to padded as a blank separator between the centered message block and the quick tasks section.
- Append the quick tasks header and task lines to `padded`.

The final `return strings.Join(padded, "\n")` is unchanged.

**Change 3 — new test in model_test.go:**
Add `TestView_ArchivedOnlyWithQuickTasks` in `internal/tui/tree/model_test.go`, immediately after the existing `TestView_ArchivedOnly` test. The test must:

1. Construct a `ProjectData` with:
   - `ArchivedMilestones` non-empty (at least one milestone)
   - `Phases` empty (`nil` or `[]parser.Phase{}`)
   - `QuickTasks` containing at least one task (e.g. status `parser.StatusTodo`, DisplayName `"Buy milk"`)
2. Build a model with that data (same pattern as `TestView_ArchivedOnly`).
3. Call `View(80, 999)`.
4. Assert:
   - `strings.Contains(output, "Quick tasks")` — header is present
   - `strings.Contains(output, "├──") || strings.Contains(output, "└──")` — at least one connector is present
   - `!strings.Contains(output, "/gsd:quick")` — static hint is absent when quick tasks exist
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/gsd-watch && go build ./internal/tui/tree/... && go test ./internal/tui/tree/... -run . -count=1</automated>
  </verify>
  <done>
    - `go build` passes with no errors.
    - `go test ./internal/tui/tree/...` passes, including `TestView_ArchivedOnlyWithQuickTasks`.
    - When `QuickTasks` is non-empty and `Phases` is empty and `ArchivedMilestones` is non-empty: View() output contains "Quick tasks" header and at least one connector line.
    - When `QuickTasks` is empty in the same state: View() output contains "or run a quick task".
    - "No GSD project found" branch output is identical to before.
  </done>
</task>

</tasks>

<verification>
go build ./internal/tui/tree/... — must compile clean
go test ./internal/tui/tree/... — all existing tests pass, including new TestView_ArchivedOnlyWithQuickTasks
Manual smoke check: build binary, open sidebar in a repo with archived milestones + quick tasks and confirm the live task list renders in the empty state pane.
</verification>

<success_criteria>
- Empty state with archived milestones + quick tasks renders the quick tasks tree, not a static hint.
- Empty state with archived milestones + no quick tasks retains the "/gsd:quick" hint.
- Both archived-milestone states start 2 lines below the pane top.
- No regressions in existing tree tests.
- TestView_ArchivedOnlyWithQuickTasks passes, covering the new rendering branch.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-on8-fix-quick-tasks-visibility-when-no-activ/260328-on8-SUMMARY.md`
</output>
