---
phase: quick-260328-he9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/styles.go
  - internal/tui/theme_test.go
  - internal/tui/styles_test.go
  - internal/tui/tree/view.go
autonomous: true
requirements: [THEME-VISUAL-DIFFERENTIATION]

must_haves:
  truths:
    - "Each theme preset (default, minimal, high-contrast) produces visibly distinct badge/label rendering"
    - "Phase stage badges ([disc], [plan], [exec], [vrfy], [uat]) are colored per-theme, not plain text"
    - "High-contrast theme makes badges bold and brightly colored for SSH/degraded terminals"
    - "Minimal theme renders badges in subdued/muted tones consistent with its content-first aesthetic"
    - "Default theme uses distinct colors for each badge category (discussion=cyan, planning=blue, execution=magenta, verification=green, testing=yellow)"
  artifacts:
    - path: "internal/tui/styles.go"
      provides: "Badge color styles per theme, expanded Theme struct with BadgeStyle field"
      contains: "BadgeStyle"
    - path: "internal/tui/tree/view.go"
      provides: "Badge rendering uses theme BadgeStyle instead of plain text"
      contains: "BadgeStyle"
    - path: "internal/tui/theme_test.go"
      provides: "Tests verifying badge styles differ across themes"
      contains: "TestBadgeStyle"
  key_links:
    - from: "internal/tui/styles.go"
      to: "internal/tui/tree/view.go"
      via: "Theme.BadgeStyle used in badge rendering loop"
      pattern: "th\\.BadgeStyle"
    - from: "internal/tui/styles.go"
      to: "internal/tui/styles.go"
      via: "BadgeString now accepts theme for styling"
      pattern: "BadgeString.*Theme"
---

<objective>
Make theme presets visually distinct by adding per-theme badge/stage-label styling and differentiating color palettes.

Currently all three themes (default, minimal, high-contrast) render phase stage badges ([disc], [plan], [exec], [vrfy], [uat]) as plain unstyled text or with only highlight/pending overrides. The themes also share very similar color palettes for status icons. This plan:
1. Adds a BadgeStyle map to Theme so each badge category gets a distinct color per theme
2. Updates BadgeString to accept and apply theme styling
3. Differentiates the three theme presets with meaningfully distinct palettes (not just minor color tweaks)
4. Makes high-contrast theme use bold + bright ANSI colors for maximum visibility

Purpose: Users who switch themes should immediately see the difference. Stage labels should be a key visual differentiator.
Output: Updated styles.go, view.go, and tests confirming visual differentiation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/tui/styles.go
@internal/tui/tree/view.go
@internal/tui/tree/model.go
@internal/tui/theme_test.go
@internal/tui/styles_test.go
@internal/tui/app/model.go
@internal/config/load.go

<interfaces>
From internal/tui/styles.go:
```go
type Theme struct {
    Complete     lipgloss.Style
    Active       lipgloss.Style
    Pending      lipgloss.Style
    Failed       lipgloss.Style
    NowMarker    lipgloss.Style
    RefreshFlash lipgloss.Style
    QuitPending  lipgloss.Style
    Highlight    lipgloss.Style
    EmptyFg      lipgloss.TerminalColor
    HelpBorder   lipgloss.TerminalColor
    HelpFg       lipgloss.TerminalColor
}

func ThemeByName(name string) (Theme, bool)
func ThemeDefault() Theme
func ThemeMinimal() Theme
func ThemeHighContrast() Theme
func StatusIcon(status string, noEmoji bool, theme Theme) string
func BadgeString(badge string, noEmoji bool) string
```

From internal/tui/tree/model.go:
```go
type Options struct {
    NoEmoji bool
    Theme   tui.Theme
}
```

Badge categories (from BadgeString):
- discussed -> [disc]
- researched -> [rsrch]
- ui_spec -> [ui]
- planned -> [plan]
- executed -> [exec]
- verified -> [vrfy]
- uat -> [uat]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BadgeStyle to Theme and update BadgeString signature</name>
  <files>internal/tui/styles.go, internal/tui/theme_test.go, internal/tui/styles_test.go</files>
  <behavior>
    - Test: ThemeDefault BadgeStyle renders [disc] differently from [exec] (different ANSI output)
    - Test: ThemeMinimal BadgeStyle renders all badges in muted tones (243/245 range)
    - Test: ThemeHighContrast BadgeStyle renders all badges with Bold=true
    - Test: BadgeString(badge, noEmoji, theme) returns styled string when noEmoji=true
    - Test: BadgeString(badge, noEmoji, theme) returns emoji (unstyled) when noEmoji=false (emoji mode unchanged)
    - Test: All three themes produce different Render() output for the same badge
  </behavior>
  <action>
1. Add a `BadgeStyle` field to `Theme` of type `map[string]lipgloss.Style`. Keys are badge names ("discussed", "researched", "ui_spec", "planned", "executed", "verified", "uat").

2. Update `BadgeString` signature from `BadgeString(badge string, noEmoji bool) string` to `BadgeString(badge string, noEmoji bool, theme Theme) string`. In emoji mode (noEmoji=false), return emoji as before (no styling change). In noEmoji mode, apply `theme.BadgeStyle[badge]` to the bracketed text if the key exists, otherwise return plain text.

3. Define distinct badge color palettes per theme:

   **ThemeDefault** — Use 256-color ANSI codes for distinct category coloring:
   - discussed/researched: Cyan (36) — discovery/information gathering
   - ui_spec: Blue (33) — design
   - planned: Blue (69) — planning
   - executed: Magenta (133) — action/shipping
   - verified: Green (34) — confirmation
   - uat: Yellow (178) — testing/caution

   **ThemeMinimal** — All badges in a single muted tone (243), no bold. Consistent with content-first.

   **ThemeHighContrast** — Bold + bright 16-color ANSI only:
   - discussed/researched: Bright Cyan (14)
   - ui_spec/planned: Bright Blue (12)
   - executed: Bright Magenta (13)
   - verified: Bright Green (10)
   - uat: Bright Yellow (11)

4. Also differentiate the main status styles more between themes:
   - **ThemeDefault**: Keep current (green/amber/red/gray) — this is fine as-is.
   - **ThemeMinimal**: Make Active use color 248 (slightly brighter than 243 Complete) so active phases stand out slightly. Keep everything else muted.
   - **ThemeHighContrast**: Add Underline to Active style (bold+underline+green) so active items pop even more. Use Bright White (15) for Pending instead of plain White (7).

5. Update all call sites of `BadgeString` to pass the theme:
   - `internal/tui/tree/view.go` line ~189: already has `th` in scope, pass it
   - Search for any other call sites (help overlay in app/model.go)

6. Update `styles_test.go` TestBadgeString tests to pass `tui.ThemeDefault()` as third arg.

7. Add new tests in `theme_test.go`:
   - `TestBadgeStyle_DefaultDistinct`: Verify at least 3 different badge categories produce different Render() output in default theme
   - `TestBadgeStyle_HighContrastBold`: Verify all high-contrast badge styles have Bold=true
   - `TestBadgeStyle_ThemesDiffer`: Verify same badge produces different output across all 3 themes
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/gsd-watch && go test ./internal/tui/... -run "TestBadge|TestTheme" -v -count=1</automated>
  </verify>
  <done>Theme struct has BadgeStyle map; BadgeString accepts theme param; all three themes define distinct badge palettes; high-contrast uses bold+bright colors; existing tests updated and new differentiation tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire badge styling into tree view and help overlay</name>
  <files>internal/tui/tree/view.go, internal/tui/app/model.go</files>
  <action>
1. In `internal/tui/tree/view.go`, update the badge rendering block (lines ~188-205) to pass `th` to `tui.BadgeString(badge, t.opts.NoEmoji, th)`. The theme `th` is already resolved at the top of View() via `themeFor(t.opts)`.

2. In `internal/tui/app/model.go`, find the helpView function that renders badge examples in the help overlay. Update any `tui.BadgeString(...)` calls to include the theme. The help overlay constructs badge examples for the legend — these should also reflect the active theme's colors.

3. Search for ALL remaining call sites of `BadgeString` across the codebase (grep for "BadgeString") and update each one to pass the appropriate theme. This includes:
   - Any test files that call BadgeString directly
   - The renderedRowLines function in view.go (line ~439) which checks BadgeString for non-empty — pass theme there too

4. Run the full test suite to confirm no compilation errors and no regressions.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/gsd-watch && go build ./... && go test ./... -count=1</automated>
  </verify>
  <done>All BadgeString call sites pass theme; tree view renders styled badges per active theme; help overlay badge legend reflects active theme colors; full test suite passes with zero failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Visually distinct theme presets with per-theme badge coloring, differentiated status palettes, and bold/bright high-contrast styling</what-built>
  <how-to-verify>
    1. Build: `cd /Users/radu/Developer/gsd-watch && go build -o gsd-watch ./cmd/gsd-watch`
    2. Run with default theme: `./gsd-watch --debug` — check that phase stage badges ([disc], [plan], [exec], etc.) show distinct colors (cyan for discussion, magenta for execution, green for verified, etc.)
    3. Run with minimal theme: `./gsd-watch --theme minimal` — check that badges are uniformly muted/gray, status icons are subdued, overall feel is content-first
    4. Run with high-contrast theme: `./gsd-watch --theme high-contrast` — check that badges are bold+bright, easily readable, distinctly colored with bright ANSI colors
    5. Compare: switching between themes should produce an immediately obvious visual difference — not just minor tweaks
    6. Press `?` in each theme to verify the help overlay badge legend also reflects the active theme colors
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test ./... -count=1` passes all tests
- `go vet ./...` reports no issues
- Visual comparison across all 3 themes shows distinct badge coloring
</verification>

<success_criteria>
- Three theme presets produce meaningfully different visual output, not just minor color tweaks
- Phase stage badges ([disc], [plan], [exec], [vrfy], [uat]) are colored per-theme
- High-contrast theme uses bold + bright 16-color ANSI for maximum visibility
- Minimal theme uses uniformly muted tones for content-first aesthetic
- Default theme uses distinct colors per badge category (discovery=cyan, planning=blue, execution=magenta, verification=green, testing=yellow)
- All existing tests pass, new tests verify cross-theme differentiation
- No regressions in emoji mode (emoji badges remain unstyled unicode characters)
</success_criteria>

<output>
After completion, create `.planning/quick/260328-he9-audit-and-improve-theme-presets-to-be-vi/260328-he9-SUMMARY.md`
</output>
